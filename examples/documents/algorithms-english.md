# Graph traversal and sorting

Dijkstra's algorithm finds shortest paths from one source when edge weights are non-negative.

Running Dijkstra with a priority queue improves practical performance on sparse graphs.

Quicksort partitions values around a pivot. Merge sort combines already sorted sequences.
